<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="banner">
	<select id="selectAllByStatus" resultType="bannerVO">
		select banner_id, banner_name, banner_path, banner_system_name 
		from tbl_banner 
		where banner_status = 1
	</select>
	
	<select id="selectAll" resultType="bannerVO">
		select banner_id, banner_name, banner_path, banner_system_name, banner_status
		from tbl_banner
	</select>
	
	<select id="select" parameterType="long" resultType="bannerVO">
		select banner_id, banner_name, banner_path, banner_system_name, banner_status
		from tbl_banner
		where banner_id = #{bannerId}
	</select>
</mapper>


